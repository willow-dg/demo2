spring:
  application:
    name: demo2
  datasource:
    url: jdbc:mysql://localhost:3306/demo2?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 7200 # 2h
    refreshTokenExpiration: 604800 # 7d
stripe:
  secretKey: ${STRIPE_SECRET_KEY}
WebsiteUrl: http://localhost:4242


